# Arbre de migrations du SCHÉMA TENANT (appliqué à CHAQUE base tenant par le runner).
# Usage manuel : uv run alembic -c apps/api/alembic.tenant.ini revision --autogenerate -m "..."
# L'URL de connexion vient de app.core.config (variables d'env) — JAMAIS de ce fichier.
[alembic]
script_location = %(here)s/migrations/tenant
