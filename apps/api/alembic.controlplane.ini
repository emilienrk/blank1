# Arbre de migrations du SCHÉMA CONTROL-PLANE (catalogue, identités, memberships).
# Usage manuel : uv run alembic -c apps/api/alembic.controlplane.ini revision --autogenerate -m "..."
# L'URL de connexion vient de app.core.config (variables d'env) — JAMAIS de ce fichier.
[alembic]
script_location = %(here)s/migrations/controlplane
