# Arbre de migrations UNIQUE (base unique + tenant_id, ADR 0001).
# Usage manuel : uv run alembic -c apps/api/alembic.ini revision --autogenerate -m "..."
# L'URL de connexion vient de app.core.config (variables d'env) — JAMAIS de ce fichier.
[alembic]
script_location = %(here)s/migrations
